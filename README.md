Setup steps:
- Git clone this repo into your system
- Install the `uv` python environment manager [here](https://docs.astral.sh/uv/)
    - For modifications to the Python environment, refer to the docs in the link
- Run: `uv run --with jupyter jupyter lab` to get the Jupyter lab environment up and running
